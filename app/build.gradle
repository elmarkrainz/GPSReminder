apply plugin: 'com.android.application'

android {
    compileSdkVersion 19
    buildToolsVersion '25.0.0'

    defaultConfig {
        applicationId "at.nasca.gpsreminder"
        minSdkVersion 16
        targetSdkVersion 25
    }

    buildTypes {
        release {
        //    minifyEnabled false
            //   proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
